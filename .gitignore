# macOS
.DS_Store

# Editor / tool state
.claude/

# Build artifacts (regenerated by `node build/build.js`)
build/data.json

# Source data — large, regenerable from upstream
source/

# Design working files — not needed for deploy
design/
